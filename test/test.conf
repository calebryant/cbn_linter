filter {
    if [event_json] != "" {
        if [event_json][src_port] != "" {
            mutate {
                replace => { 
                    "event_json.src_port" => "integer"
                }
                on_error => "zerror.src_port_is_int"
            }
        }
    }
}